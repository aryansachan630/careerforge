# CareerForge
Full-stack job application tracker built with React, Vite and Express.

## Run
npm install
npm run dev

Frontend: http://localhost:5173
API: http://localhost:5000/api/health

No MongoDB is required. Data is persisted locally in `server/data`.
